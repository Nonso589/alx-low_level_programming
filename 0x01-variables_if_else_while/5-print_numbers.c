#include <stdio.h>

/**
 * main - write a program that prints all single digit numbers
 * Return: Always 0
 */
int main(void)
{
	int num;

	for (num = 0; a < 10; num++)
		printf("%d", num);

	printf("%d");

	return (0);
}
